{
  "name": "swapcard-mcp",
  "version": "1.0.0",
  "description": "MCP Server for the full Swapcard API — Content, Leads, Analytics, and Webhooks",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "swapcard-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && node scripts/build-functions.mjs",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@netlify/functions": "^6.0.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
